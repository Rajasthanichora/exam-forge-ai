// Supabase is disabled for mobile local-only mode.
// All data is stored locally via AsyncStorage.
// Remove/ignore this file — kept as stub to prevent import errors.

export const supabase = null;

export async function execSql(_sql: string): Promise<any> {
  return null;
}

export default supabase;
